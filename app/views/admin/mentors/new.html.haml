%h1 New Mentor
= simple_form_for [:admin, @mentor] do |f|
  = render 'shared/errors', object: @mentor
  = render 'admin/shared/form_nested', f: f
  .form-group
    = f.submit 'Create New', :class => 'btn btn-primary'
