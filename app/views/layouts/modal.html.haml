.modal.fade
  .modal-dialog.modal-dialog-centered
    .modal-content
      .modal-header
        %h3.modal-title= (yield :modal_title) || page_title
        %button.close{data: { dismiss: "modal" }, aria: { label: "Close" }, type: "button" }
          %span{aria: { hidden: "true" }} ×
          %span.sr-only Close
      .modal-errors.container
      = yield